import React, { useEffect } from 'react'

const DateView = props => {

  useEffect(() => {

  }, [])

  return (
    <div>
      {props.month}
      {props.date}
    </div>
  )
}

export default DateView